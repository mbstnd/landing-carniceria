---
import Instagram from "@/icons/Instagram.astro"
const currentYear = new Date().getFullYear()

const socialLinks = [
  {
    name: "Instagram",
    url: "https://instagram.com/carniceriasanmartin",
    Icon: Instagram,
  },
]
---

<footer class="relative mt-20 w-full pb-20 pt-14 md:pt-16">
  <div class="container mx-auto px-6">
    <div class="flex flex-col place-items-center gap-16">
      <div
        class="flex w-full flex-col items-center justify-between gap-16 md:flex-row"
      >
        <div class="flex flex-col items-center gap-8 md:items-start">
          <a href="/" class="text-2xl font-bold text-white">
            Carnicería San Martin
          </a>
          <p class="text-center text-gray-400 md:text-left">
            La mejor calidad en carnes desde 2015, con la distinción de
            categoría V Nacional
          </p>
        </div>

        <div class="flex flex-col items-center gap-8 md:items-end">
          <div class="flex flex-col items-center gap-4 md:items-end">
            <h3 class="text-xl font-bold text-white">Contacto</h3>
            <a
              href="mailto:info@carniceriasanmartin.com"
              class="text-gray-400 hover:text-primary transition-colors"
            >
              info@carniceriasanmartin.com
            </a>
          </div>

          <div class="flex flex-col items-center gap-4 md:items-end">
            <h3 class="text-xl font-bold text-white">Síguenos</h3>
            <div class="flex items-center gap-4">
              {
                socialLinks.map(({ url, Icon, name }) => (
                  <a
                    href={url}
                    target="_blank"
                    aria-label="Instagram de Carniceria San Martin, se abrirá en una nueva pestaña"
                    rel="noopener noreferrer"
                    class="text-white hover:text-primary transition-colors"
                    aria-label={name}
                  >
                    <Icon class="h-10 w-10 transition-transform duration-300 hover:scale-125 " />
                  </a>
                ))
              }
            </div>
          </div>
        </div>
      </div>

      <div
        class="flex w-full flex-col items-center justify-between gap-8 border-t border-gray-800 pt-8 md:flex-row"
      >
        <p class="text-center text-sm text-gray-400 md:text-left">
          &copy; {currentYear} Carnicería San Martin. Todos los derechos reservados.
        </p>
        <div class="flex items-center gap-8">
          <a
            href="/privacidad"
            class="text-sm text-gray-400 hover:text-primary transition-colors"
          >
            Política de Privacidad
          </a>
          <a
            href="/terminos"
            class="text-sm text-gray-400 hover:text-primary transition-colors"
          >
            Términos y Condiciones
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>
